
# 20221101

root : 뷰가 가지고 있는 최상위 컴포넌트.
보간법 : 부분을 연결한다.

어떤 데이터가 변경 될 때마다 새롭게 계산되는 Data를 위한 속성이 computed이다.
computed는 확장된 데이터라고 보면 된다.

뷰인스턴스에 선언한 el, data, methods 등을 Vue가 알아서 풀어 헤쳐서 Vue instance의 프로퍼티로 풀어헤쳐서 넣어준다. 그래서 메소드 정의 할 때 화살표 함수 쓰면 안된다. 풀어 헤쳐서 들어가면 화살표 함수의 this는 렉시컬인 window를 가리키기 때문.

메소드 정의 할 때 화살표 함수 쓰지 마세요~

vue3 독스는 ctrl k 누르면 검색.

computed는 가공된 데이터
watch는 변화가 생겼을 때 실행 할 함수. computed보다 무거운 작업을 할 때. 데이터만 만드는 것이 아닌, 변경 될 때마다 실행되어야 하는 무거운 작업이 있을 때 사용. ex) 서버에서 값을 가져오거나 하는 것이 무거운 작업의 예시.

computed는 어떤 태그의 아이디를 바꾸는 등에 사용될 때가 있다.

v-for 는 값을 순회한다.
v-for와 v-if를 한 태그 안에 쓰지 마라. 하나의 요소 내에서 쓰지 마라.

template 태그는 임시로 돌릴 수 있는 공간. 태그이다.

뷰가 for를 돌릴 때 key로 돌리기 때문에, vue가 내부적으로 빠르게 돌게 하기 위해서 key를 적어주는 것이 권장사항이다. 즉 key는 v-for가 더 효율적으로 돌아가게 하기 위한 것.

template 태그에 key를 넣지 말라는 에러가 뜬다. key는 찐태그에 붙여주면 될듯? 내부에 태그 하나 만들어서.





event parameter를 명시적으로 넣어주는 방법?

axios


































