
cs에 db에 join이라는 개념이 잇다.

내부적으로 테이블 두개를 합치는 것이 join이다. 임시적으로. 데이터를 가져오기 위해서.

어떻게 합치느냐 : FK로 잡아둔 두개의 테이블 데이터를 하나로 합쳐서 쿼리를 날리는 방식이다.

inner join outer join left join 등등 쪼인이 많다.


댓글 지울 때 데이터 보관하고 싶을 때는 모델에서
is_deleted = boolean 상태로 있는데 이 때 어떻게 잘 써서 활용하기 위해 저장 해둘 수 있다. -> 구글링 하면 될듯

싸피 커리큘럼에 배포가 없다.

배포 맛보기 후반에 해봐라.